let guess = 0;
let secretNumber = 5;

while (guess !== secretNumber) {
    Guess = Number("guess the number")
    if (guess == secretNumber) {
        console.log("crt guess")
    }
    else {
        console.log("try again")
    }
}
